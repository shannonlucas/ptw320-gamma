const config = {
  root: 'src',
  logLevel: 'info',
  base: '/ptw320-gamma/',
  build: {
    outDir: '../dist',
    emptyOutDir: true,
  },
  css: {
      postcss: './config'
  }
};

export default config;
